#pragma once

#define WIN32_LEAN_AND_MEAN             // 거의 사용되지 않는 내용을 Windows 헤더에서 제외합니다.
#define NOMINMAX                        // windows.h의 min/max 매크로가 std::min/max와 충돌하지 않도록 합니다.
// Windows 헤더 파일
#include <windows.h>
